#!/usr/bin/env bash
set -e

case "${1:-}" in
  purge)
    echo "Les données utilisateur (~/.local/share/classification-presse) et la base PostgreSQL 'presse' sont conservées volontairement."
    ;;
esac

exit 0
